# Makevars file for Rcpp compilation
# This ensures proper compilation flags and linking

PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Enable C++11 standard
CXX_STD = CXX11

# Optimization flags
PKG_CXXFLAGS += -O3 -Wall -pedantic

# Include directories
PKG_CXXFLAGS += -I$(R_HOME)/include

# Link libraries
PKG_LIBS += -lRcpp
