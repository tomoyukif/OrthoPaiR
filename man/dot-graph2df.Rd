% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_functions_reorgOrthopairs.R
\name{.graph2df}
\alias{.graph2df}
\title{Enumerates connected vertex sets with at most one vertex per species
(\code{lab_genome} via \code{nm2g}), drops sets strictly contained in another,
and writes \code{orthopair_list.tsv} (columns \code{sort(unique(lab_genome))},
cells = vertex ids). Pairwise tables use original gene symbols.}
\usage{
.graph2df(
  graph,
  out_dir,
  working_dir = NULL,
  n_threads = 1L,
  max_components = Inf
)
}
\arguments{
\item{working_dir}{Project root (parent of \code{reorg_out} and \code{input}). Used
to map vertex genome IDs (\code{folder_index + 1000}) to \code{input} folder species names.}

\item{n_threads}{Number of cores for per-component enumeration (Linux/macOS; serial on Windows or when \code{1}).}

\item{max_components}{See \code{max_components} on \code{reorgOrthopairs()}.}
}
\description{
Enumerates connected vertex sets with at most one vertex per species
(\code{lab_genome} via \code{nm2g}), drops sets strictly contained in another,
and writes \code{orthopair_list.tsv} (columns \code{sort(unique(lab_genome))},
cells = vertex ids). Pairwise tables use original gene symbols.
}
